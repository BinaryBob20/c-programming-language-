#include <stdio.h>
int sumOfNaturalNumbers(int );
int main() {
    int n = 25, sum = sumOfNaturalNumbers(n);
    printf("The sum of the first %d natural numbers is: %d\n", n, sum);
    return 0;
}
int sumOfNaturalNumbers(int n) {
    if (n <= 1) {
        return n;
    } else {
        return (n + sumOfNaturalNumbers(n - 1));
    }
}